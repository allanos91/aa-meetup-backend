import { useSelector } from "react-redux"
import { useEffect } from "react"
import { useNumPastEvents } from '../../context/PastEvents'

const PastEvents = (id) => {
    const {numPastEvents, setNumPastEvents} = useNumEvents()
}
